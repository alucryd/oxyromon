# common
.DS_Store
.env
.env.*
!.env.example
.idea
.vscode
graphify-out

# rust
*.rs.bk
target
*.db
*.db-shm
*.db-wal
lcov.info
dist
*.profraw
.cargo
tests/.tmp*

# frontend (leptos + trunk)
frontend/target
frontend/dist
frontend/node_modules
# fetched at build time by frontend/scripts/fetch-webawesome.sh
frontend/vendor
.claude/skills

# desktop (tauri)
desktop/target
desktop/gen
desktop/binaries

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
